Place your exported `best.onnx` file in this directory.

The application expects the ONNX model to be available at `/models/best.onnx`.

> **Note**: For YOLOv8-OBB, export your model using:
> `yolo export model=best.pt format=onnx`
